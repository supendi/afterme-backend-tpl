This folder contains the implementations of Repository (storage) Layers. Such as : SQL server repository, postgres etc.

The MySql folder and Postgres folder are empty folders. They are just examples that we can implement repository by using another database engine.